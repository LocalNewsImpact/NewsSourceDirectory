-r requirements.txt

# Pinned exactly, and CI reads the pins from this file. A newer ruff locally than
# in CI means "passes locally, fails in CI" — which is how the F811 that shadowed
# Outlet.identity_key reached main.
pytest==9.1.1
pytest-django==4.14.0
pytest-cov==7.1.0
ruff==0.16.3
pre-commit==4.6.2

# The suite's coverage floor. `make test` runs lnic_contracts.coverage_floor
# over coverage.xml; the number lives there, not in pyproject.
lnic-contracts @ https://github.com/LocalNewsImpact/lnic-contracts/archive/refs/tags/v0.3.0.tar.gz
