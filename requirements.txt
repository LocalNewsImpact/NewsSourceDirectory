# Runtime. Bounds rather than pins until M2 installs them for real and a lock
# file exists.

Django>=5.2.17,<6
psycopg[binary]>=3.3.4
gunicorn>=26.2.0
whitenoise>=6.12.0
dj-database-url>=3.1.2

# The admin is most of the application.
django-import-export>=4.4.1      # bulk CSV/xlsx with a dry-run diff
django-simple-history>=3.13.0     # per-field history and revert
# The [socialaccount] extra, not the bare package. The Google provider needs
# requests and PyJWT[crypto] to verify the ID token signature, and neither is a
# dependency of django-allauth itself. Declaring them one production crash at a
# time is how this was discovered twice.
django-allauth[socialaccount]>=65.19.1

# Import pipeline.
pandas>=3.0.5
openpyxl>=3.1.5                  # .xlsx sources

# No boto3: the feed publishes to GitHub Pages, committed by a workflow, not to
# S3. It was 30MB of a library nothing imports.
