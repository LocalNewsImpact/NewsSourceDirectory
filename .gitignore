__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/
.env
*.sqlite3
.DS_Store
node_modules/
dist/
staticfiles/

# NOT data/ — the publish workflow commits it, and Pages serves it from there.
# It was ignored briefly while testing locally, which made the first publish
# fail with "the following paths are ignored by one of your .gitignore files".

# Playwright and coverage artefacts
/test-results/
/playwright-report/
/blob-report/
.coverage
coverage.xml
htmlcov/
